border-1px($color)
  position: relative
  &:after
    display: block
    position: absolute
    left: 0
    bottom: 0
    width: 100%
    border-top: 1px solid $color
    content: ' '
//将&:after的display置为none(因为使用border-1px()会在当前元素(ul下的li)下面加一条线,需求:最后一个元素不需要线。
//覆盖掉border-1px中display: block属性)
border-none()
  &:after
    display none
//适配不同的屏幕密度,显示不同粗细的线条,使用方式,在引入此style文件后，给相应的标签添加border-1px的class
@media (-webkit-min-device-pixel-ratio: 1.5), (min-device-pixel-ratio: 1.5)
  .border-1px
    &::after
      -webkit-transform: scaleY(0.7)
      transform: scaleY(0.7)

@media (-webkit-min-device-pixel-ratio: 2), (min-device-pixel-ratio: 2)
  .border-1px
    &::after
      -webkit-transform: scaleY(0.5)
      transform: scaleY(0.5)

bg-image($url)
  background-image url($url + '@2x.png')
  @media (-webkit-min-device-pixel-ratio: 3),(-webkit-min-device-pixel-ratio:3)
    //像素密度为3的时候使用后缀为3x的图片
    background-image url($url + '@3x.png')

